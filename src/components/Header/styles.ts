import styled from 'styled-components';

export const Title  = styled.header`
        background: #7159c1;
        height: 60px;
        color: #fff;
        display: flex;
        align-items: center;
    `;